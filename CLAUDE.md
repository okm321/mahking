# Mahking

麻雀の対局記録アプリ（モノレポ）。

## 構成

- `go/` — バックエンド（Go, module `github.com/okm321/mahking/go`）
- `web/` — フロントエンド（TanStack Start + React 19 + Vite）。詳細は `web/CLAUDE.md`
- `ops/` — DBマイグレーション（Atlas）, Terraform, コンテナ
- `.github/` — CI/CD
